import { Module } from '@nestjs/common';

/**
 * Escrow-payments bounded context — Moyasar Split integration.
 * Hold (authorize) → Capture → Refund + payouts.
 * Implementation lands in Phase 1.
 */
@Module({})
export class EscrowPaymentsModule {}
