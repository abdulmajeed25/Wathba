import { Injectable } from '@nestjs/common';

/**
 * SECURITIES-ADJACENT. DO NOT REGISTER WITHOUT A LICENSE.
 * This class is only constructed when `INVESTMENT_ENABLED=true`.
 */
@Injectable()
export class InvestmentService {
  // Intentionally empty — the dormant context exists for the schema only.
}
