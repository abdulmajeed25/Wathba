/**
 * STAKES/S-13 (G10) — consolidated: the implementation moved to
 * `@/lib/i18n/format` (one formatting module). This re-export keeps the
 * existing import sites working.
 */
export { formatSar } from '@/lib/i18n/format';
