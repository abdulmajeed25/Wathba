import { WathbaTabSkeleton } from '@/components/ventures/wathba/wathba-tab-skeleton';

/** TABS — slim content-area skeleton (header + tab bar stay persistent). */
export default function Loading() {
  return <WathbaTabSkeleton />;
}
